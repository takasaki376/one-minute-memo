高優先

- src/components/session/HandwritingCanvas.tsx (line 119)
  約30KBで、描画エンジン、ResizeObserver、SVGプレビュー、エクスポート、ツールバーUIが同居しています。
  まずは useHandwritingCanvasEngine、HandwritingToolbar、pure関数群あたりに分けるのが現実的です。iPad対策が多く入っているので、挙動変更なしの小分けリファクタが安全です。

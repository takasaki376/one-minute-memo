いいですね、履歴一覧があると「ちゃんと溜まっていく感」が出て楽しくなります。
{{/history}} 用に、いつものノリで *開発タスクをやることベースで整理*します。

----

## 🏷 タスク名

*履歴一覧画面開発（*{{src/app/history/page.tsx}}*）*

----

## 1. 画面仕様の整理

1. 履歴一覧画面で表示したい情報を決める
#* 各セッションごとのカード（{{SessionCard}}）
#** セッション開始日時（{{startedAt}}）
#** セッション終了済みかどうか（{{endedAt}} の有無）
#** 書いたメモの数（{{memoCount}}）
#** 使用テーマ数（{{themeIds.length}}）
#* 画面上部：タイトル「履歴一覧」＋簡単な説明
#* 空状態の表示（セッションが1件もない場合のメッセージ）
1. セッションの並び順を決める
#* デフォルト：{{startedAt}} or {{endedAt}} の*降順（新しい順）*

----

## 2. Repository 側の準備タスク（IndexedDB）

1. {{sessionsRepo}} に一覧取得用関数を追加
#* {{getAllSessions(): Promise<SessionRecord[]>}}
1. 必要であればソートも Repository 内でやるか、呼び出し側（画面）でやるか決める
#* MVPでは画面側で {{sort}} のほうが分かりやすい

※ 例イメージ（実装は別タスクでOK）：

```export async function getAllSessions(): Promise<SessionRecord[]> {
  const db = await getDB();
  const tx = db.transaction('sessions', 'readonly');
  const all = await tx.store.getAll();
  await tx.done;
  return all as SessionRecord[];
}
```

----

## 3. SessionCard コンポーネントのインターフェース設計

1. {{src/components/history/SessionCard.tsx}} の props を決める
#* {{id: string}}
#* {{startedAt: string}}
#* {{endedAt: string | null}}
#* {{memoCount: number}}
#* {{themeCount: number}}
1. クリックしたときに {{/history/[id]}} に遷移したいので、
#* {{href}} を受け取るか、
#* or {{onClick}} を受け取るか、
を決める（MVPでは {{href}} のほうがシンプル）

例：

```interface SessionCardProps {
  id: string;
  startedAt: string;
  endedAt: string | null;
  memoCount: number;
  themeCount: number;
}
```

----

## 4. レイアウト＆UIタスク（一覧ページ）

1. {{src/app/history/page.tsx}} に基本レイアウトを作成
#* {{max-w-3xl mx-auto px-4 py-8}} など
1. 上部ヘッダーを実装
#* タイトル「履歴一覧」
#* 説明文「これまでのセッションの記録です」など
1. セッション一覧部分のレイアウトを決める
#* 縦に {{SessionCard}} を並べる（{{flex flex-col gap-3}} など）

----

## 5. データ取得＆表示ロジックタスク

1. {{useEffect}} で {{getAllSessions()}} を呼び出し、State に保存
#* {{sessions: SessionRecord[]}}
#* {{loading: boolean}}
#* {{error: string | null}}
1. 取得したセッションをソートしてから表示
#* {{endedAt}} があるもの → {{endedAt}} 降順
#* {{endedAt}} が null（未完了）のものは {{startedAt}} 降順、または先頭にまとめるルールを決める
1. {{SessionRecord}} → {{SessionCardProps}} への変換ロジックを実装
#* {{themeCount = themeIds.length}}
#* {{memoCount}} はフィールドそのもの

----

## 6. 空状態・ローディング・エラー表示タスク

1. ローディング中の表示
#* 「履歴を読み込んでいます…」など
1. セッションが1件もない場合の表示
#* メッセージ例：「まだセッション履歴がありません」
#* 「セッションを始める」ボタンで {{/session}} へ
1. エラー時の表示
#* エラーメッセージ＋「再読み込み」or「トップへ戻る」の導線

----

## 7. ナビゲーション導線タスク

1. 各 {{SessionCard}} をクリックしたときに {{/history/[id]}} に遷移するようにする
#* {{Button}} + {{href}} でもよいし、{{next/link}} を内側で使ってもよい
1. 上部 or 下部に「セッションを開始」ボタンを置くか検討
#* 置く場合：{{<Button href="/session">セッションを開始</Button>}}

----

## 8. 日付フォーマット・表示タスク

1. {{startedAt}} / {{endedAt}} の表示形式を決める
#* 例：{{2025-11-30 22:10}} のような形式
1. 日付フォーマット用の小さなユーティリティを作るかどうか
#* {{src/lib/date/formatSessionDate.ts}} のような関数にするか、
#* MVPでは {{new Date(startedAt).toLocaleString()}} を直接使うか決める

----

## 9. 動作確認タスク

1. 1セッションだけ終えた状態で確認
#* {{/history}} に1件だけ表示されること
#* {{SessionCard}} をクリックすると {{/history/[id]}} に遷移すること
1. 複数セッションを実行した状態で確認
#* 新しいセッションが上に表示されること
1. セッションが0件の状態で {{/history}} にアクセスしたとき、空状態UIが出ること

----

## ✅ 完了条件（DoD）

- {{/history}} にアクセスすると、過去のセッションが*新しい順にカード形式で一覧表示*される
- セッションが0件のときも破綻せず「履歴がありません」といった案内が出る
- 各セッションから {{/history/[id]}} の詳細画面へ遷移できる導線がある

----

このあとに続けるなら：

- {{getAllSessions()}} の具体的な IndexedDB 実装

あたりを順に詰めていくと、履歴周りが完成形に近づきます。どこからいきましょう？

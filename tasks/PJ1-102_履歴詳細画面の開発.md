いい流れですね、履歴周りの最後のピースです。
{{/history/[id]}} 用に *履歴詳細画面の開発タスク*を整理します。

----

## 🏷 タスク名

*履歴詳細画面開発（*{{src/app/history/[id]/page.tsx}}*）*

----

## 1. 表示仕様の整理

1. 履歴詳細画面で表示したい情報を決める
#* セッション概要
#** 開始日時（{{startedAt}}）
#** 終了日時（{{endedAt}}）
#** メモ件数（{{memoCount}}）
#** 使用テーマ数（{{themeIds.length}}）
#* テーマごとのメモ一覧（10件想定）
#** 表示順：{{order}} 昇順
#** テーマタイトル・カテゴリ
#** テキストメモ本体
#** 手書きメモのサムネイル or リンク（{{handwritingDataUrl}}）
#* 画面上部タイトル：「セッション詳細」または「履歴詳細」
1. どの粒度でカード化するか決める
#* 1テーマ1カード（{{MemoItem}} コンポーネント）で縦並びにする方針にする

----

## 2. URL パラメータ・ルーティングタスク

1. App Router で {{params.id}} を取得する方法を確認
#* {{type PageProps = { params: { id: string } }}}
#* {{export default function HistoryDetailPage({ params }: PageProps)}} で受け取る
1. {{id}} が {{SessionRecord.id}} と一致する前提で扱うことを明記

----

## 3. Repository 側準備タスク（セッション＋メモ取得）

1. {{sessionsRepo}} に以下の関数を追加するタスク
#* {{getSessionById(sessionId: string): Promise<SessionRecord | undefined>}}
1. {{memosRepo}} に以下の関数を追加するタスク
#* {{getMemosBySessionId(sessionId: string): Promise<MemoRecord[]>}}
1. {{themesRepo}} に、テーマ情報取得関数がなければ追加
#* {{getThemesByIds(themeIds: string[]): Promise<ThemeRecord[]>}}
#* または {{getAllThemes()}} からフィルタでもOK（件数少ない前提なら）
1. メモ取得時のソート方針を決める
#* Repo で {{order}} 昇順にソートするか
#* or 画面側で {{memos.sort((a, b) => a.order - b.order)}} するか

----

## 4. データ結合ロジック設計タスク

1. 画面側で利用する「表示用型」を定義
#* 例：
```interface DisplayMemo {
  id: string;
  order: number;
  themeId: string;
  themeTitle: string;
  themeCategory?: string;
  textContent: string;
  handwritingDataUrl?: string;
}
```
1. 以下の処理を設計
#* {{session = getSessionById(id)}}
#* {{memos = getMemosBySessionId(id)}}
#* {{themes = getThemesByIds(session.themeIds)}}
#* {{themeMap = new Map(theme.id -> theme)}}
#* {{DisplayMemo[]}} に変換してソート（{{order}}）

----

## 5. UI レイアウトタスク

1. {{src/app/history/[id]/page.tsx}} のレイアウト構成を決める
#* 上部：セッション概要カード（{{SessionSummary}} 的な感じ）
#* 中央：メモ一覧（{{MemoItem}} のリスト）
1. {{src/components/history/MemoItem.tsx}} の props を決める
#* {{order}}, {{themeTitle}}, {{themeCategory}}, {{textContent}}, {{handwritingDataUrl}} など
1. {{MemoItem}} の見た目を決める
#* 見出し：{{「#1 テーマタイトル」}}
#* テキストメモを本文として表示
#* 手書きがある場合は、
#** 小さめの {{<img src={handwritingDataUrl} />}} を表示するか
#** 「手書きメモを表示」というリンクでモーダルに出す構成にするか、いずれか決める

----

## 6. ローディング・エラー・空状態タスク

1. セッション情報読み込み中の表示
#* 「セッション情報を読み込んでいます…」
1. セッションが見つからない場合の表示
#* 「対象のセッションが見つかりませんでした」
#* 「履歴一覧へ戻る」ボタン
1. メモが0件のケース（ありうる）の表示
#* 「このセッションにはメモが保存されていません」など

----

## 7. ナビゲーション導線タスク

1. 画面上 or 下部にナビゲーションボタンを配置
#* 「履歴一覧に戻る」 {{/history}}
#* 「もう一度セッションを始める」 {{/session}}
1. {{Button}} コンポーネントを使って統一された見た目にする

----

## 8. 日付＆時間表示タスク

1. 開始／終了時刻の表示形式を決める
#* 開始だけ → {{2025-11-30 22:15 開始}}
#* 開始〜終了 → {{2025-11-30 22:15〜22:25}}
1. 所要時間の表示を入れるか検討
#* {{endedAt}} がある場合のみ、{{(endedAt - startedAt)}} の分数表現を出すかどうか

----

## 9. データ取得実装タスク（ページ内）

1. {{page.tsx}} 内で {{params.id}} を受け取り、
#* {{useEffect}} で async 関数を呼ぶ or
#* 将来的に Server Component + {{async}} で書く方針も検討（MVPでは Client でOK）
1. 以下のステートを定義
#* {{session: SessionRecord | null}}
#* {{memos: DisplayMemo[]}}
#* {{loading: boolean}}
#* {{error: string | null}}

----

## 10. 動作確認タスク

1. 1セッション・複数メモでの確認
#* {{/history}} → セッションカードクリック → {{/history/[id]}}
#* テーマ順にメモが表示されること
1. 手書きあり／なしの両方が期待どおり表示されること
1. 存在しない {{id}} で {{/history/xxxx}} に直接アクセスしたときの挙動
1. セッション直後に {{/session/complete}} → 「履歴一覧を見る」→ 対象セッションを開く流れがスムーズか確認

----

## ✅ 完了条件（DoD）

- {{/history/[id]}} にアクセスすると、そのセッションに紐づく
** セッション概要
** テーマごとのテキスト＆手書きメモ
が一覧表示される
- セッションが取得できない場合でも、エラーメッセージ＋履歴一覧への導線が表示される
- 履歴一覧 → 詳細 → 再セッション といった一連のナビゲーションが成立している

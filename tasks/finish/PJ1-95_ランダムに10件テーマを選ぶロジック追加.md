いいところ攻めますね。
*「ランダムに10件テーマを選ぶロジック」を実装するためのタスクを、いつもどおりやることベース*で整理します。

----

## 🏷 タスク名

*有効テーマからランダムに10件選ぶロジック実装*

----

## 1. インターフェース設計タスク

1. ロジックを置く場所を決める
#* 例：{{src/lib/session/pickRandomThemes.ts}} など
1. 関数のインターフェースを決める
#* 例：
#** {{pickRandomThemes(themes: ThemeRecord[], count: number): ThemeRecord[]}}
#* もしくは「DBアクセス込み」にして
#** {{pickRandomActiveThemes(count: number): Promise<ThemeRecord[]>}}
1. どちらの形にするか方針を決める（テストしやすさ優先）

----

## 2. 入力・出力仕様の定義タスク

1. 入力となるテーマ配列（{{ThemeRecord[]}}）の前提を整理する
#* すでに {{isActive === true}} のみが渡される前提か？
1. 出力仕様を決める
#* 返却するのはテーマの *配列*
#* 配列内の順序：
#** 完全ランダムの順番にする
#** もしくは「抽選したあとで、title順などに並べ直す」か
1. 「同じテーマを重複選択しない」ことを仕様として明記する

----

## 3. エッジケース対応タスク

1. 有効テーマ数 {{< count}} の場合の挙動を決める
#* 例：
#** 有効テーマが 7 件 → 7 件すべて返す
#** 足りない分を重複で埋めない（※MVPではこれがシンプル）
1. 有効テーマ数 {{0}} 件の場合の挙動を決める
#* 空配列を返す
#* 呼び出し側（セッション画面）で「有効なテーマがありません」メッセージを表示する方針にする
1. {{count}} が 0 以下 or 異常値のときの扱いを決める
#* 内部で {{Math.max(count, 0)}} にするか、assert して早期 return か

----

## 4. ランダム選択アルゴリズム設計タスク

1. 採用するアルゴリズムを決める
#* 例：
#** Fisher–Yates シャッフルで全体をランダム化 → {{slice(0, count)}}
#** またはランダムインデックスを重複チェックしながら引き続ける
1. ランダムの偏りよりも「実装のシンプルさ」を優先するかどうか決める
#* MVPではシャッフル＋{{slice}} がわかりやすい

----

## 5. 実装タスク（ロジック部分）

1. ランダムシャッフルのユーティリティ関数を実装する
#* 例：{{shuffleArray<T>(items: T[]): T[]}}
1. {{pickRandomThemes}} 内で
#* 入力配列をコピーする（破壊しない）
#* シャッフルする
#* {{slice(0, count)}} して返す処理を書く

----

## 6. DB連携（activeテーマ取得）タスク

1. {{getActiveThemes()}} を呼び出して {{ThemeRecord[]}} を取得する関数を用意する
#* 例：{{pickRandomActiveThemes(count: number)}}
1. 上記で取得したテーマ配列に対して {{pickRandomThemes}} を適用する
1. セッション開始画面 or セッションページでこの関数を呼び出し、
#* {{selectedThemes}} を state にセットする処理を組み込む

----

## 7. テスト・検証タスク

1. 以下のケースで手動 or ユニットテストを設計する
#* 有効テーマが 0 件
#* 有効テーマが 5 件、{{count=10}}
#* 有効テーマが 10 件、{{count=10}}
#* 有効テーマが 20 件、{{count=10}}
1. テストの観点を整理する
#* 戻り値の長さが {{min(themes.length, count)}} になっているか
#* 同じテーマIDが重複していないか
#* 同じ入力配列から複数回呼び出したとき、順番が変化する可能性があるか

----

## 8. セッション開始フローとの統合タスク

1. セッション開始時の処理において
#* {{const themes = await getActiveThemes();}}
#* {{const selected = pickRandomThemes(themes, 10);}}
#* {{createSession(selected.map(t => t.id));}}
のフローを組み込む
1. 選ばれた10件のテーマを、セッション画面の {{currentTheme}} 表示に連携する

----

## ✅ 完了条件（DoD）

- 任意の {{ThemeRecord[]}} から、重複なしで最大10件のランダムテーマが取得できる
- 有効テーマ数が10件未満でも破綻せず動作する
- 有効テーマが0件の場合は空配列が返り、呼び出し側で適切なメッセージに分岐できる
- セッション開始時に、実際にランダムなテーマセットで 1〜10 テーマが進行できる

----

この次は：

- {{pickRandomThemes}} の *具体的な TypeScript 実装サンプル*
- または {{useCountdown}} のインターフェースとタスク分解

どちらかを一緒に詰めていきましょうか？

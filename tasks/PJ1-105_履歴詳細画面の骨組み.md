いいですね、履歴詳細までつながるとアプリとしてかなり気持ちよくなります。
{{/history/[id]/page.tsx}} *の骨組みコード*を用意しました。

- 前提：
** {{getSessionById}}（{{sessionsRepo}}）
** {{getMemosBySessionId}}（{{memosRepo}}）
** {{getThemesByIds}}（{{themesRepo}}、なければ TODO コメント）
** {{Button}} コンポーネント
- やっていること：
** {{params.id}} からセッションID取得
** Session＋Memo＋Theme を読み込む
** テーマ順にメモ一覧を表示
** ローディング／エラー／空状態の枠を用意

----

## 📄 {{src/app/history/[id]/page.tsx}}（骨組み）

```'use client';

import React, { useEffect, useMemo, useState } from 'react';

import { Button } from '@/components/ui/Button';
import { getSessionById } from '@/lib/db/sessionsRepo';
import { getMemosBySessionId } from '@/lib/db/memosRepo';
// TODO: テーマのタイトル/カテゴリを表示したい場合に利用
// import { getThemesByIds } from '@/lib/db/themesRepo';

import type { SessionRecord } from '@/types/session';
import type { MemoRecord } from '@/types/memo';
import type { ThemeRecord } from '@/types/theme';

type LoadStage = 'idle' | 'loading' | 'loaded' | 'error';

interface PageProps {
  params: {
    id: string;
  };
}

/** 表示用にまとめた1件分のメモ */
interface DisplayMemo {
  id: string;
  order: number;
  themeId: string;
  themeTitle: string;
  themeCategory?: string;
  textContent: string;
  handwritingDataUrl?: string;
}

export default function HistoryDetailPage({ params }: PageProps) {
  const sessionId = params.id;

  const [stage, setStage] = useState<LoadStage>('idle');
  const [session, setSession] = useState<SessionRecord | null>(null);
  const [memos, setMemos] = useState<MemoRecord[]>([]);
  const [themes, setThemes] = useState<ThemeRecord[]>([]);
  const [error, setError] = useState<string | null>(null);

  // データ読み込み
  useEffect(() => {
    const load = async () => {
      try {
        setStage('loading');
        setError(null);

        const loadedSession = await getSessionById(sessionId);

        if (!loadedSession) {
          setError('対象のセッションが見つかりませんでした。');
          setStage('error');
          return;
        }

        const loadedMemos = await getMemosBySessionId(sessionId);

        // TODO: テーマ情報を IndexedDB で管理している場合のみ有効化
        // const loadedThemes = await getThemesByIds(loadedSession.themeIds);

        setSession(loadedSession);
        setMemos(loadedMemos);
        // setThemes(loadedThemes);
        setStage('loaded');
      } catch (e) {
        console.error('Failed to load history detail', e);
        setError('履歴の読み込み中にエラーが発生しました。');
        setStage('error');
      }
    };

    void load();
  }, [sessionId]);

  // themeId -> theme のMap（なければダミー）
  const themeMap = useMemo(() => {
    const map = new Map<string, ThemeRecord>();
    for (const t of themes) {
      map.set(t.id, t);
    }
    return map;
  }, [themes]);

  // 表示用メモリストに変換
  const displayMemos: DisplayMemo[] = useMemo(() => {
    if (!session) return [];

    return memos.map(memo => {
      const theme = themeMap.get(memo.themeId);
      return {
        id: memo.id,
        order: memo.order,
        themeId: memo.themeId,
        themeTitle: theme?.title ?? `テーマ ${memo.order}`,
        themeCategory: theme?.category,
        textContent: memo.textContent,
        handwritingDataUrl: memo.handwritingDataUrl,
      };
    }).sort((a, b) => a.order - b.order);
  }, [memos, session, themeMap]);

  // ローディング中
  if (stage === 'idle' || stage === 'loading') {
    return (
      <main className="mx-auto max-w-3xl px-4 py-8">
        <h1 className="text-xl font-semibold text-slate-900">
          セッション履歴を読み込んでいます…
        </h1>
        <p className="mt-2 text-sm text-slate-600">
          少しお待ちください。
        </p>
      </main>
    );
  }

  // エラー or セッションなし
  if (stage === 'error' || !session) {
    return (
      <main className="mx-auto max-w-3xl px-4 py-8">
        <h1 className="text-xl font-semibold text-slate-900">
          セッション履歴を表示できません
        </h1>
        {error && (
          <p className="mt-2 text-sm text-slate-600">
            {error}
          </p>
        )}
        <div className="mt-6 flex flex-wrap gap-3">
          <Button href="/history" variant="primary">
            履歴一覧に戻る
          </Button>
          <Button href="/session" variant="secondary">
            新しくセッションを始める
          </Button>
          <Button href="/" variant="ghost">
            トップへ戻る
          </Button>
        </div>
      </main>
    );
  }

  // 正常時: セッション概要
  const started = session.startedAt
    ? new Date(session.startedAt)
    : null;
  const ended = session.endedAt ? new Date(session.endedAt) : null;

  const startedLabel = started
    ? started.toLocaleString()
    : '不明';
  const endedLabel = ended ? ended.toLocaleString() : '進行中';

  return (
    <main className="mx-auto max-w-3xl px-4 py-8">
      {/* ヘッダー */}
      <header className="border-b border-slate-200 pb-4">
        <h1 className="text-2xl font-semibold text-slate-900">
          セッション履歴の詳細
        </h1>
        <p className="mt-2 text-sm text-slate-600">
          このセッションで書き出したメモの一覧です。
        </p>
      </header>

      {/* セッション概要 */}
      <section className="mt-6 rounded-lg border border-slate-200 bg-white p-4 shadow-sm">
        <h2 className="text-sm font-medium text-slate-800">
          セッション概要
        </h2>
        <dl className="mt-3 grid grid-cols-1 gap-2 text-sm text-slate-700 sm:grid-cols-2">
          <div className="flex flex-col">
            <dt className="text-xs text-slate-500">開始時刻</dt>
            <dd>{startedLabel}</dd>
          </div>
          <div className="flex flex-col">
            <dt className="text-xs text-slate-500">終了時刻</dt>
            <dd>{endedLabel}</dd>
          </div>
          <div className="flex flex-col">
            <dt className="text-xs text-slate-500">テーマ数</dt>
            <dd>{session.themeIds.length} 件</dd>
          </div>
          <div className="flex flex-col">
            <dt className="text-xs text-slate-500">メモ数</dt>
            <dd>{session.memoCount} 件</dd>
          </div>
        </dl>
      </section>

      {/* メモ一覧 */}
      <section className="mt-8">
        <h2 className="text-sm font-medium text-slate-800">
          メモ一覧
        </h2>

        {displayMemos.length === 0 ? (
          <p className="mt-3 text-sm text-slate-600">
            このセッションにはメモが保存されていません。
          </p>
        ) : (
          <ul className="mt-4 flex flex-col gap-4">
            {displayMemos.map(memo => (
              <li
                key={memo.id}
                className="rounded-lg border border-slate-200 bg-white p-4 shadow-sm"
              >
                <header className="flex flex-col gap-1 border-b border-slate-100 pb-2">
                  <div className="flex items-center justify-between text-xs text-slate-500">
                    <span># {memo.order}</span>
                    {memo.themeCategory && (
                      <span className="rounded bg-blue-50 px-2 py-0.5 text-[11px] font-medium text-blue-700">
                        {memo.themeCategory}
                      </span>
                    )}
                  </div>
                  <h3 className="text-sm font-semibold text-slate-900">
                    {memo.themeTitle}
                  </h3>
                </header>

                <div className="mt-3 text-sm text-slate-800 whitespace-pre-wrap">
                  {memo.textContent || (
                    <span className="text-slate-400">
                      テキストメモはありません。
                    </span>
                  )}
                </div>

                {memo.handwritingDataUrl && (
                  <div className="mt-3">
                    <p className="mb-1 text-xs font-medium text-slate-600">
                      手書きメモ
                    </p>
                    <div className="overflow-hidden rounded-md border border-slate-200 bg-slate-50">
                      <img
                        src={memo.handwritingDataUrl}
                        alt="手書きメモ"
                        className="block max-h-64 w-full object-contain"
                      />
                    </div>
                  </div>
                )}
              </li>
            ))}
          </ul>
        )}
      </section>

      {/* フッターの導線 */}
      <section className="mt-8 flex flex-wrap gap-3 border-t border-slate-200 pt-4">
        <Button href="/history" variant="secondary">
          履歴一覧に戻る
        </Button>
        <Button href="/session" variant="primary">
          新しくセッションを始める
        </Button>
      </section>
    </main>
  );
}
```

----

## 🔍 後から肉付けしやすいポイント

- {{themes}} の取得部分は今は {{TODO}} にしてあるので、
** テーマを IndexedDB で持つようになったら {{getThemesByIds(session.themeIds)}} を差し込めばOK
- {{DisplayMemo}} に必要な項目が増えたら、そこだけ拡張すれば UI 側がすっきり保てます
- この骨組みのままでも、
** 「セッション概要」
** 「テーマ順のメモ一覧」
は最低限表示できます

----
